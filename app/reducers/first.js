import Axios from 'axios';

//Action Types


//Action Creators


//Thunk Creators


//Reducer
const firstReducer = (state = {}, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default firstReducer;
